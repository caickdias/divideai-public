const DB_NAME = '@divideai'

const CUSTOMERS_COLLECTION = `${DB_NAME}:customers`;
const ITEMS_COLLECTION = `${DB_NAME}:items`;
const CURRENT_TABLE = `${DB_NAME}:currentTable`;
const TABLE_HISTORY = `${DB_NAME}:tableHistory`;

export {
    CUSTOMERS_COLLECTION,
    ITEMS_COLLECTION,
    CURRENT_TABLE,
    TABLE_HISTORY,
}